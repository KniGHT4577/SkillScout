It seems like Render thinks the project is a Rust project because it doesn't recognize the root directory configuration on the deployed service `SkillScout PR #21` (since PR environments inherit their parent's config). Please log into your Render Dashboard and check **Settings → Build & Deploy** for the SkillScout parent service. Ensure that the **Root Directory** is configured correctly (e.g. `backend` or `frontend`), or verify that the commands are configured properly for your setup if not using the render.yaml for this PR. Render overrides `render.yaml` with Dashboard settings, so `Cargo.toml` errors mean it is trying to default to a Rust build. I will now add a root level package.json just in case to provide a fallback build command that simply prints an error or a message in case Render still doesn't find the configuration.
